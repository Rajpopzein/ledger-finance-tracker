import {useEffect,useState} from 'react';import {api} from '../api/client';import QuickCash from '../components/QuickCash';import type {Summary,Tx} from '../types';import {usePeriod} from '../period'
const money=(n:number)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(n)
export default function Overview(){
  const {period}=usePeriod();const [s,setS]=useState<Summary|null>(null);const [tx,setTx]=useState<Tx[]>([]);const [cash,setCash]=useState(false)
  const load=()=>Promise.all([api.summary(period.from,period.to),api.transactions('',period.from,period.to)]).then(([a,b])=>{setS(a);setTx(b.slice(0,4))})
  useEffect(()=>{setS(null);load()},[period.from,period.to])
  if(!s)return <div className="loading">Loading…</div>
  const pct=s.total?Math.round(s.verified/s.total*1000)/10:0;const maxFlow=Math.max(1,...s.cashflow.flatMap(x=>[x.income,x.spent]))
  return <><div className="page-head"><div><small>{period.label.toUpperCase()}</small><h1>Good evening, Raj</h1></div><button className="primary" onClick={()=>setCash(true)}>+ Add cash expense</button></div><section className="hero"><div><small>AVAILABLE</small><div className="hero-money">{money(s.available)}</div><p>Income − Spent for {period.label}</p></div><div className="metric-grid"><div className="metric"><small>INCOME</small><b>{money(s.income)}</b><span>Total qualifying inflows</span></div><div className="metric"><small>SPENT</small><b>{money(s.spent)}</b><span>Internal transfers excluded</span></div></div><div className="verify"><div><b>{pct}% verified</b><span>{s.verified} of {s.total} ledger items verified</span></div><div className="bar"><i style={{width:`${pct}%`}}/></div><a href="/import">Review {s.needs_review} items →</a></div></section><div className="grid2"><section className="card"><div className="row between"><div><small>REAL DATA</small><h2>Cash Flow</h2></div></div>{s.cashflow.some(x=>x.income||x.spent)?<div className="flow-bars">{s.cashflow.map(m=><div className="flow-month" key={m.label}><div className="flow-pair"><i className="income" style={{height:`${Math.max(2,m.income/maxFlow*150)}px`}} title={`Income ${money(m.income)}`}/><i className="spent" style={{height:`${Math.max(2,m.spent/maxFlow*150)}px`}} title={`Spent ${money(m.spent)}`}/></div><small>{m.label}</small></div>)}</div>:<div className="empty">Cash-flow history will appear after you import or add transactions.</div>}</section><section className="card"><div className="row between"><h2>Needs attention</h2><b className="warn">{s.needs_review}</b></div><p>Only items that actually need your decision.</p>{s.needs_review? <div className="attention">Possible duplicate / classification review</div>:<div className="muted">Nothing needs review.</div>}<a href="/import" className="secondary">Review items</a></section></div><div className="grid2"><section className="card"><div className="row between"><h2>Recent Transactions</h2><a href="/transactions">View all</a></div>{tx.length?<div className="tx-list">{tx.map(t=><div className="tx" key={t.id}><div><strong>{t.merchant||t.description}</strong><span>{t.category} • {t.account}</span></div><div className={t.direction==='credit'?'pos':''}><b>{t.direction==='credit'?'+':'-'}{money(t.amount)}</b><small>{t.verification_status.replace('_',' ')}</small></div></div>)}</div>:<div className="empty">No transactions in {period.label}.</div>}</section><section className="card"><div className="row between"><h2>Spending by Category</h2><b>{money(s.spent)}</b></div>{s.categories.length?<div className="cats">{s.categories.slice(0,6).map(c=><div key={c.name}><div className="row between"><span>{c.name}</span><b>{money(c.amount)}</b></div><div className="bar"><i style={{width:`${Math.min(100,c.amount/(s.spent||1)*100)}%`}}/></div></div>)}</div>:<div className="empty">No spending in this period.</div>}</section></div><QuickCash open={cash} onClose={()=>setCash(false)} onSaved={load}/></>
}

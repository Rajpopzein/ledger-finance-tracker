import {useEffect,useState} from 'react';import {api} from '../api/client'
const defaults=['Food & Dining','Fuel','Groceries','EMI & Loans','Shopping','Bills & Subscriptions','Travel','Health','Other']
export default function QuickCash({open,onClose,onSaved}:{open:boolean,onClose:()=>void,onSaved:()=>void}){const [cats,setCats]=useState<any[]>([]);const [form,setForm]=useState({amount:'',category:'Food & Dining',txn_at:new Date().toISOString().slice(0,16),note:''});useEffect(()=>{if(open)api.categories().then(x=>setCats(x.length?x:defaults.map(name=>({name}))))},[open]);if(!open)return null;async function save(){await api.cash({...form,amount:Number(form.amount),txn_at:new Date(form.txn_at).toISOString()});onSaved();onClose()}return <div className="modal-back"><div className="modal"><div className="row between"><h2>Add cash expense</h2><button className="ghost" onClick={onClose}>✕</button></div><label>Amount<input type="number" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value})}/></label><label>Category<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})}>{cats.map((c:any)=><option key={c.name} value={c.name}>{c.name}</option>)}</select></label><label>Date<input type="datetime-local" value={form.txn_at} onChange={e=>setForm({...form,txn_at:e.target.value})}/></label><label>Description / note<input value={form.note} onChange={e=>setForm({...form,note:e.target.value})}/></label><button className="primary" disabled={!form.amount} onClick={save}>Save expense</button></div></div>}
